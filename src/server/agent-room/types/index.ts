/**
 * Agent Room domain types — re-exported from the canonical shared module.
 */

export type {
  AgentSession,
  PhaseBufferEntry,
  SwarmFriction,
  TurnDirectiveResult,
  PhaseAdvanceResult,
  SpeakerSelectionParams,
  TurnPromptParams,
  SwarmUnit,
  SwarmStatus,
  SwarmVote,
  TurnVote,
} from '@/shared/domain/agentRoom.types';
